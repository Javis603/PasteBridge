# PasteBridge uses different .env files for each side.
#
# On the Mac server:
#   cp .env.server.example .env
#
# On the Windows client:
#   copy .env.client.example .env
#
# Keep AUTH_TOKEN the same on both machines.
